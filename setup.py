from distutils.core import setup

setup(name='iterativeplot',
      version='1.0dev',
      description='Python package for iterative plotting during exploratory data analysis',
      author='Patrick Long',
      packages=['iterativeplot'],
      package_dir = {'': 'src'},
        )
